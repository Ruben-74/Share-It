/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, //double les consoles il faut le desactiver
  //dans le cas ou on utilise next export avec firebase
  // images: {
  //   unoptimized:true,
  // }
};

export default nextConfig;
